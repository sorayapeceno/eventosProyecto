package dam.primero.modelos.logistica.controlador;

import dam.primero.exception.MyException;
import dam.primero.modelos.logistica.modelo.Mercancia;
import dam.primero.repositorio.logistica.Repositorio_Mercancias;

import java.sql.SQLException;
import java.time.LocalDate;

public class GestionaLogistica {
/*
    public static void main(String[] args) {
        try {
            Mercancia mercancia1 = new Mercancia(
                    4,
                    "Auriculares Bluetooth",
                    59.99,
                    "Electronica",
                    "Unidad",
                    10,
                    45,
                    LocalDate.of(2026, 5, 22)
            );

            Repositorio_Mercancias repo = new Repositorio_Mercancias();
            repo.Registromercancia(mercancia1);

        } catch (MyException e) {
            System.err.println("Error de conexión a la base de datos: " + e.getMessage());
        } catch (SQLException e) {
            System.err.println("Error en la operación de base de datos: " + e.getMessage());
        }
    }*/
}